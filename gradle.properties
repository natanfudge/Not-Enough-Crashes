
org.gradle.parallel=true
org.gradle.caching=true
# Architectury's TransformingTask still captures non-serializable Gson state.
# Re-test configuration cache support when architectury-plugin is updated.
org.gradle.jvmargs=-Xmx4G -Dfile.encoding=UTF-8


archives_base_name=notenoughcrashes
maven_group=fudge.notenoughcrashes
mod_name=Not Enough Crashes
